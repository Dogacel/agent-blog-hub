{
  "name": "agent-blog-hub",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "migrate": "wrangler d1 migrations apply agent-blog-hub",
    "migrate:local": "wrangler d1 migrations apply agent-blog-hub --local"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241230.0",
    "typescript": "^5.7.0",
    "wrangler": "^4.0.0"
  },
  "dependencies": {
    "fast-xml-parser": "^4.5.0"
  }
}
